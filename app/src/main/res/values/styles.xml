<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <!-- Face detector main theme. -->
    <style name="AppTheme.NoTitleBar" parent="@style/Theme.AppCompat.Light">
        <item name="windowNoTitle">true</item>
        <item name="windowActionBar">false</item>
        <item name="android:windowBackground">@android:color/background_dark</item>
        <item name="android:windowFullscreen">true</item>
        <item name="android:windowContentOverlay">@null</item>
        <item name="android:alertDialogTheme">@style/Theme.AsusRes.Dialog.Alert</item>
    </style>

    <!-- Coach editor theme -->
    <style name="ToolBarTheme" parent="Theme.AppCompat.Light">
        <!-- used to replace tool bar-->
        <item name="windowNoTitle">true</item>
        <item name="windowActionBar">false</item>
        <item name="android:windowFullscreen">?android:windowNoTitle</item>
        <item name="toolbarStyle">@style/ToolBarStyle</item>
        <item name="windowActionModeOverlay">true</item>
        <item name="actionModeStyle">@style/ActionMode</item>
        <item name="actionModeCloseDrawable">@drawable/back</item>
        <item name="android:alertDialogTheme">@style/Theme.AsusRes.Dialog.Alert</item>
    </style>

    <style name="ToolBarStyle" parent="Base.Widget.AppCompat.Toolbar">
        <item name="android:titleTextStyle">@style/ToolBarTitleStyle</item>
    </style>
    <style name="ToolBarTitleStyle" parent="TextAppearance.Widget.AppCompat.Toolbar.Title">
        <item name="android:textColor">@android:color/white</item>
        <item name="android:textSize">@dimen/editor_tool_bar_text_size</item>
    </style>

    <style name="ActionMode" parent="@style/Widget.AppCompat.ActionMode">
        <item name="background">@android:color/black</item>
        <item name="titleTextStyle">@style/ActionModeTitle</item>
    </style>

    <style name="ActionModeTitle" parent="@style/TextAppearance.AppCompat.Widget.ActionBar.Title">
        <item name="android:textColor">@android:color/white</item>
        <item name="android:textSize">@dimen/editor_tool_bar_text_size</item>
    </style>
    <!-- elements style  -->
    <style name="PopupMenuTheme" parent="ThemeOverlay.AppCompat.Dark.ActionBar">
    </style>

    <style name="CommonCloseButtonStyle">
        <item name="android:layout_alignParentBottom">true</item>
        <item name="android:layout_centerHorizontal">true</item>
        <item name="android:background">@drawable/sm_button_click_selector</item>
        <item name="android:layout_marginBottom">@dimen/close_image_bottom_margin</item>
        <item name="android:layout_width">wrap_content</item>
        <item name="android:layout_height">wrap_content</item>
    </style>

</resources>
